// Top-level build file where you can add configuration options common to all sub-projects/modules.
// This file configures the entire project build settings

plugins {
    // Android application plugin - enables Android-specific build features
    id("com.android.application") version "8.2.0" apply false

    // Kotlin Android plugin - enables Kotlin language support for Android
    id("org.jetbrains.kotlin.android") version "1.9.20" apply false
}

// No additional configuration needed at root level for this project